<%= form_for @micropost do |f| %>
  <%= render "shared/error_messages", object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: t "share.form.placeholder" %>
  </div>
  <%= f.submit t("submit.post"), class: "btn btn-primary" %>
<% end %>
